name: My First Workflow

on:
   push:
       branches:
         - master 


jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print a message
        run: echo "Hello from Github Actions!"
